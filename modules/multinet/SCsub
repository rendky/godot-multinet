Import('env')

env_multinet = env.Clone()
env_multinet.Append(CPPPATH=["#modules/multinet"])

sources = [
    "register_types.cpp",
    "terrain/terrain_adapter.cpp",
]

env_multinet.add_source_files(env.modules_sources, sources)
